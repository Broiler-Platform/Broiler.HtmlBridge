{
  "$comment": [
    "The engine-neutrality ratchet. One entry per directory under src/, recording how much of it",
    "still names a JavaScript engine directly. scripts/check-engine-neutrality.sh measures the tree",
    "and compares it against these numbers.",
    "",
    "THESE NUMBERS MAY FALL AND MAY NEVER RISE. A count above its budget fails the build, and the",
    "fix is to port the file rather than to raise the number. A migration that moves a file onto",
    "Broiler.HtmlBridge.Jseal lowers a count, and must lower the budget in the SAME COMMIT -- the",
    "check reports every count that came in under budget for exactly that reason, because a ratchet",
    "nobody tightens is a ratchet that stops at whatever the worst day was.",
    "",
    "THE ONE RISE THAT IS NOT A REGRESSION IS A CHANGE TO THE METRIC, AND THERE HAS BEEN ONE.",
    "engineReferences used to count only the string 'Broiler.JavaScript'. Every project under src/",
    "has a RootNamespace beginning 'Broiler.', so 'JavaScript.Runtime.JSObject' compiles and means",
    "the same thing -- and 23 real references in Broiler.HtmlBridge.Dom were spelled that way,",
    "invisible to the check, concentrated in exactly the files mid-migration where somebody had",
    "dropped a using and qualified what was left. The count now sees both spellings. Dom's recorded",
    "number therefore went 108 -> 111 in the commit that widened it while its actual coupling FELL by",
    "18 in the same commit, and both halves of that sentence are the point: a number may rise when",
    "the instrument improves, and it may never rise because the tree got worse. A commit doing the",
    "first says so here.",
    "",
    "The consequence worth stating plainly: a project whose budget is already 0 cannot gain an",
    "engine-coupled file at all. That is the state every entry here is travelling towards, and it",
    "is what lets the remaining migration land as a hundred small changes instead of one merge that",
    "moves 250 files and can only be reviewed by trusting it.",
    "",
    "Broiler.HtmlBridge.Jseal is 0/0/0 and is the one entry these numbers do not actually protect.",
    "Its project file has no ProjectReference and no PackageReference, so an assembly that cannot",
    "see a Broiler.JavaScript type cannot name one: its neutrality is a compiler outcome, not a grep",
    "result. What the check guards there is the CLAIM -- it fails if that project file ever gains a",
    "reference of either kind, because the moment it does, every 0 below stops meaning anything.",
    "",
    "WHAT EACH METRIC COUNTS, LITERALLY.",
    "",
    "  engineReferences   Occurrences of the text 'Broiler.JavaScript' in .cs files beneath the",
    "                     directory, bin/ and obj/ excluded. Text, not semantics: a using directive,",
    "                     a fully-qualified type name and a mention inside a doc comment each count",
    "                     one. Counting occurrences rather than files is deliberate -- a file being",
    "                     half-ported is progress, and a metric that only moved when a whole file",
    "                     landed would hide it.",
    "",
    "  engineProjectRefs  ProjectReference Include paths in the directory's .csproj files that point",
    "                     into Broiler.JS/ or Broiler.VM/, counted regardless of Condition. This is",
    "                     the metric that actually has to reach 0 for an engine to be swappable: as",
    "                     long as a binding project links the engine, nothing stops the next file",
    "                     from using it, and the two counts above are the only thing noticing.",
    "",
    "  guestEvalSites     Occurrences of '.Eval(' in .cs files. The name records where these are",
    "                     going, not what they are: today's sites are undifferentiated, and splitting",
    "                     them into IJsSource.EvaluateHostScript (source this repository authored,",
    "                     exempt from the page's content policy) and EvaluateGuestSource (source the",
    "                     page supplied, which a Content-Security-Policy may forbid) is the porting",
    "                     work this number measures. A ported site names neither .Eval( nor an engine",
    "                     type, so it leaves all three counts at once.",
    "",
    "WHY THE UNIT IS A DIRECTORY AND NOT A PROJECT FILE. src/Broiler.App has no .csproj: its eight",
    "files are pulled in by <Compile Include=\"..\\Broiler.App\\...\"> from three heads. Keying this",
    "on project files would leave those files counted nowhere, which is the one place an engine",
    "reference could be parked without any budget noticing. Keying on the directory covers them, and",
    "makes the entry name the same string the check prints.",
    "",
    "This is an object rather than the array eng/solutions.json uses, so that a directory cannot be",
    "budgeted twice -- two entries for one project would let the check read whichever came first.",
    "A project marked \"role\": \"provider\" is the one kind whose engine references are SUPPOSED to grow -- naming the engine is what a provider assembly is for. For those, engineReferences and guestEvalSites are recorded and reported when they drift, but not enforced; engineProjectRefs stays enforced, because which engine assemblies a provider links is a real boundary and a Broiler.JS provider quietly acquiring a Broiler.VM reference should have to argue for it in a diff."
  ],
  "projects": {
    "Broiler.App": {
      "engineReferences": 0,
      "engineProjectRefs": 0,
      "guestEvalSites": 0,
      "note": "Shared sources with no project file of their own; linked into the Core, Linux and Windows heads. Nothing here has ever touched the engine, and being at 0 without a csproj means it cannot acquire a reference except in source."
    },
    "Broiler.App.Android": {
      "engineReferences": 0,
      "engineProjectRefs": 0,
      "guestEvalSites": 0
    },
    "Broiler.Browser.Android": {
      "engineReferences": 0,
      "engineProjectRefs": 0,
      "guestEvalSites": 0
    },
    "Broiler.Browser.Core": {
      "engineReferences": 0,
      "engineProjectRefs": 1,
      "guestEvalSites": 0,
      "note": "The one reference is Broiler.JavaScript.All, unconditional. No source here names the engine, so this is a link the head does not use directly -- it is here because BrowserApp.NewScriptEngine() still picks an engine with an #if rather than through JsEngineRegistry. It drops to 0 when it does; see the remarks on JsEngineRegistry. The sibling reference to Broiler.HtmlBridge.Scripting.Vm is conditional on the -VM build types and is not counted, because it points inside src/."
    },
    "Broiler.Browser.Core.Tests": {
      "engineReferences": 1,
      "engineProjectRefs": 0,
      "guestEvalSites": 0,
      "note": "ONE, AND THE ARGUMENT THIS ENTRY ASKED FOR IS BELOW. It used to read: 'if a test ever needs to name one directly, that is a real budget increase and this file is where the argument for it belongs.' JsInteropSeamTests is that test. JsInterop is the seam between an engine object and a JSEAL handle, and its defect was that a function crossing it came back kind Object while the same function from the provider came back kind Function -- so two handles over one object compared unequal, which a handle-keyed map cannot survive. A test that could not name JSObject could not assert the correspondence; it could only assert that JSEAL agrees with itself. The increase is one using line and it buys the only check that would catch the defect returning. It falls back to 0 when JsInterop is deleted, which is the whole point of the programme this test serves. VmScriptEngineTests still compiles only under the -VM build types and gets the engine transitively."
    },
    "Broiler.Browser.Linux": {
      "engineReferences": 0,
      "engineProjectRefs": 0,
      "guestEvalSites": 0
    },
    "Broiler.Browser.Windows": {
      "engineReferences": 0,
      "engineProjectRefs": 0,
      "guestEvalSites": 0
    },
    "Broiler.HtmlBridge.Core": {
      "engineReferences": 1,
      "engineProjectRefs": 1,
      "guestEvalSites": 0,
      "note": "ONE FILE, ONE LINE, AND IT IS THE SEAM. The single reference is `using Broiler.JavaScript.Engine;` in Dom/IDomBridgeRuntime.cs, and the only thing it serves is the pair of Attach overloads that take a JSContext. This was 3 across two files until ScriptCompileAhead.cs was deleted -- 327 lines with no caller anywhere in the tree, whose two usings were the other 2. What that removal buys is not the count: it means engineProjectRefs 1, the ProjectReference to Broiler.JavaScript.Engine, is now held by exactly one API in one file, so an Attach that takes an IJsRealm drops the reference in the same commit rather than turning up a second dependency behind it. This is the highest-value entry in the file despite being the smallest -- an Attach a second engine can call is what makes a second engine able to serve a page at all. Note for whoever ports it: Broiler.JS's own docs/roadmap/Modernization.md MOD-M5 names the deleted file as an import source, so it is taken from this repository's history rather than from the tree."
    },
    "Broiler.HtmlBridge.Dom": {
      "engineReferences": 60,
      "engineProjectRefs": 3,
      "guestEvalSites": 2,
      "note": "111 UNDER A WIDER METRIC THAN THE 108 IT REPLACES, AND THE TREE GOT BETTER, NOT WORSE. The count now also sees engine namespaces spelled without their Broiler. root -- 23 of them here -- while the same commit deleted 20 dead using lines from the four LayoutMetrics partials and added 2 that JsInterop needs to answer the kind a provider would. Real coupling fell by 18. The DOM bridge. Started at 891 when JSEAL landed, and the name-validation cluster moved -- ThrowDOMException and the three validators now take an IJsRealm and reach IJsCalls.DomError, which the Broiler.JS provider implements with the same algorithm and the same fallback wording, so no page sees a different exception. That commit also removed IAttributesHost.JsContext, whose own doc comment had named the condition for its removal. What remains is structural rather than unmigrated: the three wrapper-root fields DomBridge declares and ten files read; IDomBridgeRuntime.Attach, which takes a JSContext from another project; JsObjectRegistry, the wrapper-identity choke point, whose surface ten files read while holding the engine's object mid-frame; BridgeModuleContext, which derives from the engine's module context because JSEAL has no module-graph contract; and the event-host surfaces that still type a listener as the engine's function. The weak tables that were recorded here as needing a reference-typed key a struct handle cannot be were a MISTAKE: the struct was never the key, the reference it carries is, and JsValue.ObjectIdentity carried five of them across with no provider change. The ArrayBuffer mint left when IJsValues grew NewArrayBuffer and TryGetArrayBufferBytes, and the Storage delete left when IJsExoticDelete landed -- both were contract gaps rather than unmigrated code, and both closed by adding to JSEAL rather than by working around it here. Each occurrence names its own pin in its doc comment. 64 -> 60 and 6 -> 5 by deleting rather than porting: DomFunction, superseded when the Broiler.JS provider took createPrototype:false for every member the bridge installs, so its 3 references were paying for a type nothing constructed; a stale using in ElementContentBinding that named the engine and used nothing from it; and RegisterNamedElementGlobals, public, JSContext-typed, carrying one of the eval sites, and with no caller at any point in this repository's history. guestEvalSites 5 -> 2: the three sub-document classic-script loops in SubDocuments.cs now go through IJsSource.EvaluateClassicScript, the third source member, rather than the engine's own evaluation entry point. engineReferences stays 60 because that file's one engine reference is a using for the module tail below those loops, which still needs a JSModuleContext the source contract does not describe. The two that remain are XmlAndScripts.cs, which is the same move on the XHTML path, and EngineModuleSupport.cs -- and that last one is the FLOOR: it does not reach 0 until JSEAL has a module contract, so a commit claiming 0 here is claiming something else happened. NOTE FOR ANYONE WRITING PROSE ABOUT THIS METRIC: guestEvalSites is textual and a mention in a comment counts as a call, so the first attempt at DomBridge.cs's replacement comment held the count at 6 by naming the method it was describing. That is the metric working as specified, not a bug in it."
    },
    "Broiler.HtmlBridge.Jseal": {
      "engineReferences": 0,
      "engineProjectRefs": 0,
      "guestEvalSites": 0,
      "note": "Proved by the compiler, not by this file. The project has no ProjectReference and no PackageReference, so it cannot name an engine type whatever a grep says; the check fails if that project file ever gains one. See the comment in Broiler.HtmlBridge.Jseal.csproj."
    },
    "Broiler.HtmlBridge.Jseal.BroilerJs": {
      "engineReferences": 43,
      "engineProjectRefs": 3,
      "guestEvalSites": 1,
      "role": "provider",
      "note": "The Broiler.JS provider, and the one project under src/ whose budget is not expected to reach zero. Naming Broiler.JavaScript is what this assembly is for: every reference here is a reference the bridge no longer has to make. The single eval site is JSContext.Eval inside the realm's own IJsSource implementation -- the provider being asked to evaluate, not a binding evaluating. Its two engine project references are Broiler.JavaScript.Engine and .BuiltIns. Its engineProjectRefs rose from 2 to 3 when IJsClone landed: JSGlobalStatic.StructuredClone lives in Broiler.JavaScript.Globals, so the provider that implements structured clone needs it. That is the gate doing its job rather than a regression -- the same reference LEFT Broiler.HtmlBridge.Dom in the same commit, because the bridge's only use of it was the call that is now behind the contract. An engine assembly reached by the provider and not by the bridge is the shape all of these should end in."
    },
    "Broiler.HtmlBridge.Jseal.Vm": {
      "engineReferences": 0,
      "engineProjectRefs": 5,
      "guestEvalSites": 0,
      "role": "provider",
      "note": "The Broiler.VM provider. It is marked a provider for the same reason the Broiler.JS one is - naming an engine is what a provider is FOR - but its reference count reads 0 rather than a large number, and the zero is a fact about the METRIC and not about this assembly. engineReferences counts occurrences of the string Broiler.JavaScript, which is the Broiler.JS engine namespace; this provider names Broiler.VM.Profile.JavaScript instead, so the count does not see it. The five project references do see it, and they are the honest measure here: Abstractions, Binary, Runtime, the profile, and the profile compiler. Read the 0 as \"this assembly names no Broiler.JS type\", which is true and worth keeping true, and read the 5 as its engine coupling."
    },
    "Broiler.HtmlBridge.Scripting": {
      "engineReferences": 9,
      "engineProjectRefs": 3,
      "guestEvalSites": 6,
      "note": "Five files now: the Broiler.JS ScriptEngine and what it needs to build a realm. InteractiveSession left the count when its constructor stopped taking a JSContext -- it read the context once, to dispose it, so the parameter is typed IDisposable and the class names no engine type at all. That reference was load-bearing for a reason unrelated to what it did: VmScriptEngine named the constructor as half of why the document-bearing overloads are forwarded. This is where the Broiler.JS provider will live once it is one, and the counts here move to a Jseal.BroilerJs assembly rather than to zero -- a provider naming its own engine is what a provider is for."
    },
    "Broiler.HtmlBridge.Scripting.Vm": {
      "engineReferences": 1,
      "engineProjectRefs": 5,
      "guestEvalSites": 0,
      "note": "The 1 is a doc comment in VmScriptEngine.cs saying the profile does not expose Broiler.JavaScript types. It is counted because the metric is textual and stays textual: a check that tried to tell prose from code would need a parser, and would then be arguable in a way this is not. Lower it by rewording the sentence, or leave it -- it costs nothing at 1."
    }
  }
}
